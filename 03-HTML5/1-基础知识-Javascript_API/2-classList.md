>新h5中DOM对象多了一个classList属性，是一个数组

#类名更改方法：
    1.添加类名
        this.classList.add();
    2.移除类名
        this.classList.remove();
    3.判断是否包含某类名
        this.classList.contains();
    4.切换类名(添加取消类名)
        this.classList.toggle();