<div>
	<a href=""></a>
	<a href=""></a>
</div>	
	// a标签之间的空白字符会产生空格，在js代码在其间插入元素并清空后a之间的空白字符也会清空，导致样式会发生抖动。
------------------------------------------
处理方法：
	1. div设置font-size:0;
	2. 设置a为inline-block，否则会自动占满一行，永远上不去
	3. a中设置font-size为相应大小。
	------------------------------------
	应用：
		两个div平分一个1024盒子