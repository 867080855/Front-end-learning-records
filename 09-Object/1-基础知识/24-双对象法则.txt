双对象法则：
    一个对象其实由两个对象组成：
        1. 构造函数对象
        2. 原型对象
======================================
两者如何联系到一起的？
    原型链
    通过_proto_属性链接到一起(chrome firfox edge)，
先在的_proto_属性已经成为了ES5的一个标准
    早期的浏览器不是通过该属性链接的
========================================
