/**
 * Created by lenovo on 2017/2/22.
 */
/*
* call:
*   由一个调用者和多个离散的参数组成参数列表。
*   call可以调用一个函数或者一个对象，调用的原则是供爷法则
* apply：
*   由一个调用者和一个argument数组组成参数列表
*   apply可以完成计算数组的最大值(Math.max.apply(null,arr))以及拼接数组(Array.prototype.push.apply(arr1,arr2))等操作
* */