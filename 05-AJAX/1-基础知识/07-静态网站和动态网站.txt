网站：一系列的网页所组成。
    1.静态网站：
    	弊端：更新维护困难
    2.动态网站(html文档可以动态生成)：
		接从数据库中调用数据动态生成html文档，网站更加利于维护。

----------------------------------------------------------------
	ajax返回的不再是以前的php和html的结合，而是开始返回一段数据。
去实现页面的无刷新动态页面。