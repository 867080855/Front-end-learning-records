get:
	问服务器要数据，会在url后边加上字符串作为数据要求
post:
	给数据库提交数据，数据库返回相应的数据，在url后边没有字符串。
------------------------------------------------------
	get不能传输大量的数据，有限制。post没有
--------------------------------------------------------
	php中的$_GET['']拿到的是地址栏中的内容？？？？