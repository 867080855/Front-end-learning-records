3.删除节点(只能移除孩子。)
    1).removeChild();
4.克隆节点(可以复制任意节点。)
    1).cloneNode();
        //括号里可以跟参数，如果为true，则为深层复制，除了复制本盒子，还复制子节点。
            //如果为false，只复制本节点，不复制子节点。
        ---------------------------------------------------------
        var cloneNode = box.children[1].cloneNode();
        box.appendChild(cloneNode);
            //注意克隆节点之后的插入操作，可以把两步合成一步来写。