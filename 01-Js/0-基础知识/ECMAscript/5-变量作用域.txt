全部变量和局部变量
    全局变量两种声明方式：
        1.在最外层声明。
            var num = 10;
        2.在函数内声明。(不加var即可)
            function fun(){
                num = 10;
            }
    局部变量
        //局部变量受保护，外部不能调用。
        1.在函数内声明。
            function fun(){
                var num = 10;
            }