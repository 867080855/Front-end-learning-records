变量与属性其实都可以称之为变量。
    不过变量是公有的变量，而属性是公有的变量。
---------------------------------------------------
如：
    1.var index = 10;
var arr = [1,2,3,4,5];
    2.arr.index = 10;
    //不可以写成
            //var arr.index = 10;
            //直接赋值就好了。相当于创造了一个属性。