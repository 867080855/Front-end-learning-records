1.动态设置一个属性
    $(".btn").attr({
        "title":"dynamic tag"
    })
    -------------------------
    js中为setAttribute
2.获取属性
    $(".btn").attr("title");
3.移除属性
    $(".btn).removeAttr("title");