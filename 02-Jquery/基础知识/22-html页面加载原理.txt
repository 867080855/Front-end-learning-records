1.浏览器输入地址值，请求html页面
2.加载html文本文件
3.逐行解析(一边下载一边解析)
4.遇到css代码加载到css树
5.遇到script代码开始加载js代码。(开始一个新的线程加载js)
    //相当于有另一个浏览器在帮我们加载js。
    //如果再遇到一个之后会再开一个。并行加载
6.一直到加载到</html>页面加载完成，但是此时并不意味这css图片的加载完成。
    //jQuery加载的速度比window.onload要快。
        //a.window.onload需要等到所有的文件加载完成才可以。
        //b.jQuery并行加载
