全局对象
    $ jQuery
---------------------------------
冲突后会怎么样？
    var $ = {};
    $ === jQuery?
    //之前的$对象会被覆盖
    ---------------------
    怎么解决？
        var myJQuery = $.noConflict();
            //让Jquery释放$,让$回到$对象上去
            $.name;